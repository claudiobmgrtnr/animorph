import babel from 'rollup-plugin-babel';
import uglify from 'rollup-plugin-uglify';

export default {
  entry: 'src/jquery.js',
  format: 'umd',
  moduleName: 'animorph',
  plugins: [
    babel({
      presets: [ [ 'es2015', { modules: false } ], 'stage-0' ],
      babelrc: false
    }),
    uglify()
  ],
  globals: {
    jquery: 'jQuery'
  },
  external: [
    'jquery'
  ],
  sourceMap: true,
  dest: 'dist/animorph.jquery.min.js'
};
